export function createRecognition({onResult,onStart,onEnd,onSilence}={}){const R=window.SpeechRecognition||window.webkitSpeechRecognition;if(!R)return null;const r=new R();r.lang='en-US';r.continuous=false;r.interimResults=true;r.maxAlternatives=3;let timer;r.onstart=()=>{onStart?.();timer=setTimeout(()=>onSilence?.(),5000)};r.onresult=e=>{clearTimeout(timer);onResult?.(Array.from(e.results).map(x=>x[0].transcript).join(' '))};r.onend=()=>{clearTimeout(timer);onEnd?.()};r.onerror=()=>{clearTimeout(timer);onEnd?.()};return {start:()=>r.start(),stop:()=>r.stop(),recognition:r}}
